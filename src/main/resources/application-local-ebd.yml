# 로컬 - embedded  모드
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:./local/data'
    username: sa
    password:
  h2:
    console:
      enabled: true # 웹 콘솔 기능 활성화
      path: /h2-console # 경로
  jpa:
    hibernate:
      ddl-auto: create
